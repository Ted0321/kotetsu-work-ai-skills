# X投稿 完成稿 — Claude Code 週次おそうじ（改良版）

`templates/thin_drop_x_post.md` の型に沿った、そのまま貼れる完成稿。
Before は 2026-08-05 実測値（合計662.5MB・ほぼ全部が会話ログ）を反映済み。

## メイン投稿（通常投稿・138.5字換算＝確定版）

```text
海外でバズったCodexの「古いキャッシュ自動クリア」。これ、Claude Codeでも再現できた。しかも、たった1プロンプトでOK。

あとはClaude Codeが毎週勝手にお掃除。放っておくと会話ログだけで数百MB〜数GB溜まるので、SSDの空きが戻り、バックアップや同期も軽くなる。

解説と導入プロンプトは↓
```

過去稿（実測662MB＋情報衛生の組み合わせ・137字換算）:

```text
Codexで話題の「古いキャッシュの週次自動掃除」、Claude Codeには無いのでスクリプトのスケジュール登録で再現しました。

あとはClaude Codeが毎週勝手にお掃除。会話ログだけで実測662MB。これが自動回収され、古いログのコード片が残り続けないのも安心。

解説と導入プロンプトはリプ欄に🧵
```

メリットの引き出し（差し替え用）: ①容量＝実測662MB・放置でGB級 ②情報衛生＝古い会話ログのコード片・機密断片が残り続けない ③バックアップ/クラウド同期が軽くなる（.claudeはホーム直下のため）

※確定版は主語を「Claude Code」にした擬人化でキャッチーさを出す（設置したのはClaude Code自身なので嘘にならない範囲）。「AIが毎週起動して確認する」とは書かない——実際の毎週実行はスクリプトのため。その仕組みはリプ3で自分からタネ明かしして強みに変える。

添付画像（1枚構成）: `assets/before-after-zero.png`
16:9スライド。左=溜まった古いデータ（数ヶ月利用の想定例2.8GB）→ 右=実行後ゼロ＋2.8GB回収。
「ゼロ」の定義は**30日より古い不要データ**（カード見出しとフッターに明記済み。直近30日分は保護のため、全データがゼロになる表現にはしていない）。
数字を変えたいときは `assets/before-after-zero.html` の数値を書き換えてブラウザで開けば同じ見た目で再生成できる。

補強リプ（任意）: `assets/before-real.png`（2026-08-05 実測662MB）を「ちなみに私の実測はこちら」と添えると、想定例と実測の両輪で信頼感が出る。

## 長文版（プレミアム長文投稿を使う場合の代替）

```text
Claude Code、使うほどローカルにデータが溜まり続けてるの、知ってました？
私の環境で実測662MB、ほぼ全部が「過去の会話ログ」。放置すれば数GBコースです。

海外でバズってた「codexに後片付けさせるプロンプト」を、Claude Code用に改良しました。

改良版がやること
・毎週の実行にAIを使わない（OSスケジューラに登録＝トークン0・確認なし）
・消す場所はパスで固定。AIの解釈に任せない
・30日より新しいものは機械的に保護
・実行前ドライラン＋Before/Afterレシート
・「止めて」の一言で解除

使い方
1. リプ2のワンライナーで自分の溜まり具合をチェック
2. リプ1のプロンプトをClaude Codeに貼る
3. 以後は毎週月曜9時に勝手に片付く

プロンプト全文はリプに🧵 保存して月曜の朝に。
```

## リプ1（本体プロンプト）

プレミアム（長文投稿）ならプロンプト全文をテキストで貼る（→ [PROMPT.md](./PROMPT.md) の「② 本体プロンプト」をコピー）。
**テキストで貼れない場合**は `assets/prompt-card.png`（プロンプト全文の画像版）を添付し、コピー用リンクを添える:

```text
本体プロンプトはこれ（画像）。
コピペ用テキストはここから↓
https://github.com/Ted0321/kotetsu-work-ai-skills/blob/main/skills/claude-cache-cleanup/PROMPT.md

貼るとまずドライラン（何がどれだけ消えるか）が表示されて、
問題なければ設置→週次登録→初回実行まで進みます。
```

## リプ2（診断ワンライナー）

```text
自分の「溜まり具合」はこれで見られます（消しはしない、見るだけ）

Mac / Linux / WSL:
cd ~ && du -shc .claude/projects .claude/todos .claude/tasks .claude/shell-snapshots .claude/file-history .claude/backups .claude/statsig .cache/claude-cli-nodejs Library/Caches/claude-cli-nodejs 2>/dev/null

Windows (PowerShell) は文字数の都合でここから↓
https://github.com/Ted0321/kotetsu-work-ai-skills/blob/main/skills/claude-cache-cleanup/PROMPT.md
```

## リプ3（安全設計＋導線）

```text
「AIに削除を任せて大丈夫？」への答えが今回の改良の本体です。

・消してよい場所を実パスで列挙（それ以外に触るコードが存在しない）
・「最近のものに触らない」は更新日時フィルタで機械的に保証
・ファイル単位の削除のみ（rm -rf 禁止）／保持7日未満は拒否
・素のClaude Codeにプロンプトだけ渡す再現テスト済み

タネ明かし：毎週の実行はAI起動ではなく仕組み化（スクリプト＋OSスケジューラ）。だからAPI代ゼロ・確認なしでも安全に、勝手に動き続けます。

検証済みスクリプト・画像素材ごとリポジトリに置いてます：
https://github.com/Ted0321/kotetsu-work-ai-skills/tree/main/skills/claude-cache-cleanup

スキルとして入れたい人は：
npx skills add Ted0321/kotetsu-work-ai-skills@claude-cache-cleanup
```

## 画像ALTテキスト案

- メイン画像（before-after-zero.png）: 「ビフォーアフター図解。30日より古い不要データ2.8GB（想定例）が、改良版プロンプト1回で0MBになり2.8GB回収。以後は毎週月曜9時に自動実行。直近30日分・設定・スキルは保護」
- 実測画像（before-real.png）: 「診断コマンドの出力。Claude Codeのローカルデータが合計662.5MB、うち662MBが過去の会話ログ」
- プロンプトカード: 「Claude Code週次おそうじの改良版プロンプト全文。消してよい場所のリストと安全ルール、5つの手順」

## After（レシート）が出たら

- 回収量が大きい場合: そのままレシートスクショを2枚目に
- 回収量が小さい場合（使用期間が30日未満・掃除済み等）: 本文は変更不要。レシートの「次回: 毎週月曜9:00に自動実行」を主役に、「溜まる前に仕込む保険」として見せる（レシート画像がそれを語ってくれる）

## 投稿前チェックリスト

- [x] Before の数字を実測値（662MB）に差し替えた
- [ ] After のレシートスクショを取得した
- [ ] プロンプトが**テキストでコピーできる導線**がある（画像のみにしない）
- [ ] リポジトリのリンク先が main ブランチにマージ済みで、リンクが生きている
- [ ] 画像にALTテキストを付けた
