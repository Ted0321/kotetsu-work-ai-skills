# X投稿 完成稿 — Claude Code 週次おそうじ（改良版）

メイン投稿（140字）＋リプ長文1本（プレミアム前提）の2階建て構成。

## メイン投稿（最終稿・139.5字換算）

280ユニット中279。**1字も足せない**ので変更する場合は必ず再計測すること。

```text
海外でバズったCodexの「古いキャッシュ自動クリア」。これ、Claude Codeでも再現できた。
やり方は1プロンプトを送るだけ。あとはClaude Codeが毎週勝手にお掃除してくれる。放っておくと会話ログだけでも数百MB〜数GB溜まるので、なんか動作重いな～と感じてる人は試してみて。解説とプロンプトは↓
```

添付画像: `assets/before-after-zero.png`（16:9スライド。30日より古い不要データ2.8GB→0MB。「ゼロ」の定義は30日より古いデータで、画像内に「直近30日分は保護」明記済み）。
数字を変えたいときは `assets/before-after-zero.html` を書き換えてブラウザで開けば再生成できる。

## リプ（長文1本・仕組み→プロンプト→補足）

プレミアムの長文投稿でそのまま1本で貼る。
（長文が使えない場合の代替: `assets/prompt-card.png` を画像添付し、PROMPT.md へのリンクを貼る）

```text
【仕組み】
メイン投稿の「1プロンプト」を送ると、Claude Codeがその場でここまでやります。

①ドライラン：どこの・何件・何MBが消せるかを表で提示（この時点では何も消さない）
②掃除スクリプトを ~/.claude/cleanup/ に設置
③OS標準のスケジューラに毎週月曜9時で登録（Windows:タスクスケジューラ／Mac:launchd／Linux:cron）
④初回のお掃除を実行して、回収量をレポート

ポイントは、毎週の実行にAIを使わないこと。毎週動くのは決定的なスクリプトだけなので、API代ゼロ・許可確認なしのまま安全に回り続けます。

消すのは「30日より古い」会話ログ・シェルスナップショット・編集履歴・MCPログ・一時ファイルだけ（スナップショットは14日）。設定・CLAUDE.md・スキル・エージェント・認証情報・直近30日のデータには一切触りません。素のClaude Codeにこのプロンプトだけを渡して、意図どおり動くことを隔離環境で検証済みです。

【導入プロンプト】↓をそのままClaude Codeに貼るだけ
＝＝＝ここから＝＝＝
Claude Code が溜め込んだ古いキャッシュ・会話ログ・一時ファイルを安全に片付ける「週次おそうじ」をセットアップして。以下を厳守すること。

【方式】
毎週の実行にAIを使わないこと。決定的なクリーンアップスクリプトを作成し、OS標準のスケジューラに毎週月曜9:00で登録する（Windows: タスクスケジューラ / macOS: launchd / Linux: cron。WSLはWindows側から wsl.exe 経由で登録）。あなたの仕事は今回の設置1回だけ。以後は確認なし・トークン消費ゼロで毎週動く。

【消してよいもの】次の場所の「30日より古いファイル」だけ（★印は14日）
・~/.claude/projects/ 内の *.jsonl（過去の会話ログ。memory/ など jsonl 以外は残す）
・~/.claude/ 内の todos/ tasks/ file-history/ backups/ statsig/
・~/.claude/shell-snapshots/ ★
・MCPログ: ~/.cache/claude-cli-nodejs（macOSは ~/Library/Caches/claude-cli-nodejs、Windowsは %LOCALAPPDATA%\claude-cli-nodejs）
・OSの一時フォルダ直下の claude-*
存在しない場所はスキップ。削除は必ずファイル単位で行い、フォルダごとの rm -rf は禁止（空になったサブフォルダの削除のみ可）。

【絶対に触らないもの】
settings.json / settings.local.json / CLAUDE.md / keybindings.json / .credentials.json / ~/.claude.json、agents・skills・commands・hooks・plugins・rules などの資産フォルダ、そして保持日数以内のファイル全部。スクリプトには「保持日数が7日未満なら実行を拒否する」安全装置を入れること。

【手順】
1. まずドライラン: 何も消さずに、削除対象を「場所/件数/サイズ」の表で見せる
2. スクリプトを ~/.claude/cleanup/ に設置（ドライラン用フラグ付き。実行のたび cleanup.log に記録を残す）
3. スケジューラへ登録（既存の同名登録は置き換えて二重登録を防ぐ）。登録結果を表示して確認
4. ~/.claude/settings.json に "cleanupPeriodDays": 30 を追記マージ（他のキーは保持。JSONが壊れていたら触らず報告）
5. 初回のおそうじを実行し、最後に必ずこの形式で報告:

━━━ Claude Code おそうじレポート ━━━
 before : X.X GB（X,XXX ファイル）
 after  : X.X GB → ★X.X GB 回収★
 内訳   : 会話ログ X.X GB / スナップショット X.X GB / MCPログ X.X GB / その他 X.X GB
 次回   : 毎週月曜 9:00 に自動実行（もう確認しません）
 停止   : 「おそうじ自動化を止めて」と言えば解除
━━━━━━━━━━━━━━━━━━━━━━
＝＝＝ここまで＝＝＝

【補足・注意点】
・対象は自分のPCのClaude Codeです（Web版のセッション内で実行しても意味がありません）
・削除したログは復元できません。30日より古い会話には claude --resume で戻れなくなるので、過去の会話をよく掘り返す人はプロンプト内の「30日」を60〜90日に書き換えて（7日未満は動かない安全装置入り）
・止めたいときは、Claude Codeに「おそうじ自動化を止めて」と言うだけ
・先に自分の溜まり具合を見たい人は、Claude Codeに「Claude Codeが溜め込んでいるデータ量を場所別に表で見せて」と聞いてみて（見るだけで消しません）
・検証済みスクリプト・診断ワンライナー・画像素材はこちら
https://github.com/Ted0321/kotetsu-work-ai-skills/tree/main/skills/claude-cache-cleanup
```

## 画像ALTテキスト案

- メイン画像（before-after-zero.png）: 「ビフォーアフター図解。30日より古い不要データ2.8GB（想定例）が、プロンプト1回で0MBになり2.8GB回収。以後は毎週月曜9時に自動実行。直近30日分・設定・スキルは保護」
- 実測画像（before-real.png・補強用）: 「診断コマンドの出力。Claude Codeのローカルデータが合計662.5MB、うち662MBが過去の会話ログ」
- プロンプトカード（prompt-card.png・長文が使えない場合）: 「Claude Code週次おそうじの改良版プロンプト全文。消してよい場所のリストと安全ルール、5つの手順」

## 補強ネタ（任意）

- `assets/before-real.png`（2026-08-05 実測662MB）を「ちなみに私の実測はこちら」とリプに追加すると、想定例と実測の両輪で信頼感が出る
- 自分のPCで本体プロンプトを実行して出た「おそうじレポート」のスクショを追加すると After の実例になる

## 投稿前チェックリスト

- [x] メイン投稿が140字以内（139.5字。1字も足せない）
- [x] プロンプト全文がテキストでコピーできる（リプ本文に掲載）
- [ ] リポジトリのリンク先が main ブランチにマージ済みで、リンクが生きている
- [ ] 画像にALTテキストを付けた
